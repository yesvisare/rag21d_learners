# M3.2 Cloud Deployment - Environment Variables Template
# Copy this to .env and replace with actual values

# Platform identifier (auto-detected on Railway/Render)
PLATFORM=local

# Admin secret - CHANGE THIS in production
ADMIN_SECRET=changeme

# Port (auto-injected by platforms, override for local dev)
PORT=8000

# Railway-specific (auto-injected)
# RAILWAY_ENVIRONMENT=production

# Render-specific (auto-injected)
# RENDER=true
# RENDER_SERVICE_NAME=m3-2-cloud-deploy
